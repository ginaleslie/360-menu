import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks"
import Icon from "./"

<Meta title="360 Menu/Icon" component={Icon} />

# Icon component

takes a name of the icon it should display
it should inherrit the color from its parrent
the should appear in different sizes

export const Template = args => <Icon {...args} />

<Canvas>
  <Story name="default Icon" args={{ name: "WAITER", color: "grayOne" }}>
    {Template.bind({})}
  </Story>
</Canvas>
